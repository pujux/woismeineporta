# SQLite database file (mounted volume in production: /data/app.db)
DATABASE_PATH=./data/app.db

# Set to 1 to start the in-process availability poller (production: 1)
ENABLE_POLLER=0
# Poll intervals in milliseconds
POLL_FAST_MS=30000
POLL_SLOW_MS=180000

# Optional: route ALL retailer fetches through a proxy, for when the server IP gets
# flagged by bot protection (MediaMarkt 403 / Amazon CAPTCHA; Bauhaus now goes via
# api.bauhaus directly, no proxy needed). e.g. the WARP sidecar in
# docker-compose.warp.yml (GOST on :1080). Leave empty = direct. See README.
# RETAILER_PROXY_URL=socks5://warp:1080

# Optional: public Bauhaus apiKey (from the PDP source: apiKey:"…"). Set this to fetch
# online status, per-store stock AND the price directly from api.bauhaus when the
# Cloudflare PDP is 403'd from your server IP — skips the PDP entirely, nothing is lost.
BAUHAUS_API_KEY=

# Bearer token for POST /api/admin/check (manual poll trigger)
ADMIN_SECRET=change-me

# Web Push VAPID keys — generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
# mailto: contact for push services
VAPID_SUBJECT=mailto:you@example.com

# Scaleway Transactional Email (email alerts, EU) — https://www.scaleway.com/en/transactional-email-tem/
# Secret key from the Scaleway console (IAM → API keys) + the Project ID.
SCALEWAY_SECRET_KEY=
SCALEWAY_PROJECT_ID=
# Optional: TEM region (fr-par | nl-ams | pl-waw); defaults to fr-par.
SCALEWAY_TEM_REGION=fr-par
# Absender — muss eine in Scaleway verifizierte (Sub-)Domain sein (SPF/DKIM)
EMAIL_FROM="Wo is meine Porta? <noreply@woismeineporta.pufler.dev>"
# Optional: Reply-To (z. B. dein PrivateEmail-Postfach), sonst kein Reply-To
EMAIL_REPLY_TO=
# Optional: Empfänger für Betriebs-Alarme (Adapter fällt aus, neuer Händler führt das
# Produkt). Fällt auf EMAIL_REPLY_TO zurück; ohne beides sind Owner-Alarme deaktiviert.
ADMIN_EMAIL=

# Public base URL — Canonical/OpenGraph, sitemap.xml/robots.txt, E-Mail-Links
PUBLIC_BASE_URL=http://localhost:3000
